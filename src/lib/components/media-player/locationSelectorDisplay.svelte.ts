export const LibraryLocationSelector = $state({
    display: false, // Whether to show the library location selector in /library page
    expanded: false, // Whether the library location selector is expanded to show the list of locations
});